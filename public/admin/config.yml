// Fichier : media-finance-crypto/tailwind.config.mjs
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      boxShadow: {
        'brutal': '4px 4px 0px 0px rgba(0,0,0,1)',
        'brutal-lg': '8px 8px 0px 0px rgba(0,0,0,1)',
        'brutal-hover': '2px 2px 0px 0px rgba(0,0,0,1)',
      },
      colors: {
        base: '#ffffff',
        surface: '#f8fafc',
        primary: '#fef08a', // Jaune vif
        secondary: '#bfdbfe', // Bleu vif
        accent: '#fbcfe8', // Rose vif
        dark: '#0f172a',
      },
      typography: (theme) => ({
        DEFAULT: {
          css: {
            '--tw-prose-body': theme('colors.dark'),
            '--tw-prose-headings': theme('colors.dark'),
            '--tw-prose-links': theme('colors.blue[600]'),
            '--tw-prose-bold': theme('colors.dark'),
            '--tw-prose-quotes': theme('colors.dark'),
            'h1, h2, h3, h4': {
              fontWeight: '900',
              textTransform: 'uppercase',
              letterSpacing: '-0.025em',
            },
            'img': {
              border: '4px solid #000',
              boxShadow: '4px 4px 0px 0px rgba(0,0,0,1)',
              borderRadius: '0',
            },
            'pre': {
              border: '4px solid #000',
              boxShadow: '4px 4px 0px 0px rgba(0,0,0,1)',
              borderRadius: '0',
            },
          },
        },
      }),
    },
  },
  plugins: [
    require('@tailwindcss/typography'),
  ],
}